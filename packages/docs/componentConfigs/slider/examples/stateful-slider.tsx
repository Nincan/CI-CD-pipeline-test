import { ReactElement } from 'react';
import { StatefulSlider } from 'spaceweb/slider';

const Stateful = (): ReactElement => <StatefulSlider initialState={{ value: [10] }} />;

export default Stateful;
