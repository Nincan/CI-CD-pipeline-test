import { CompactAutoFormat, NUMBER_VARIANTS } from '../utils/intlNumberFormatUtils/types';

export const DEFAULT_VARIANT: CompactAutoFormat = {
  type: NUMBER_VARIANTS.COMPACT_AUTO_FORMAT,
};

export const DEFAULT_CURRENCY = 'USD';
